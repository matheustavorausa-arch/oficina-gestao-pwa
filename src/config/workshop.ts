export const workshopSchedulingConfig = {
  // Change this list for each shop's operating hours.
  availableTimes: ['08:00', '10:00', '14:00', '16:00'],

  // MVP default: one customer per time slot. Increase this for shops with more bays/staff.
  maxCustomersPerTimeSlot: 1,
}
